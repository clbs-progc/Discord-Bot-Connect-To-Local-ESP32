# Discord-Bot-Connect-To-Local-ESP32


When a user sends a !attention command to either the Bot's DMs or a Server, it connects to the local server created by the ESP32 and blinks a blue light, until !off command is sent.


Make sure to set the Arduino IDE for ESP32 Dev Module.
